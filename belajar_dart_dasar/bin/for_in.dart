void main() {
  var names = ['sadam', 'alzani'];
  for (var element in names) {
    print(element);
  }

  var nameSet = {'juni', 'fitria'};
  for (var element in nameSet) {
    print(element);
  }
}
